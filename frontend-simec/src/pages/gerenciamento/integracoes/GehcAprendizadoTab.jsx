import React from 'react';
import { LoadingState } from '@/components/ui';

// Sub-aba "Aprendizado da IA" da integração GE Healthcare.
// Mostra o que a IA está fazendo com os dados do portal GE: PDFs capturados,
// causas-raiz identificadas, padrões detectados, atividade recente, e os
// controles de pausa/retomada dos pipelines automáticos.
//
// Conteúdo completo é preenchido no commit seguinte deste PR.

function GehcAprendizadoTab() {
  return (
    <LoadingState message="Aprendizado da IA — em montagem (próximo commit deste PR)." />
  );
}

export default GehcAprendizadoTab;
